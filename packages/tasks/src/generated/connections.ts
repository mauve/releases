/* eslint-disable */
/**
 * Branded service-connection types.
 *
 * Each kind corresponds to a `connectedService:<Kind>` input type in the upstream task
 * definitions. Use the constructor functions to create a typed reference to a service
 * connection by name; the brand prevents accidentally passing one connection kind where
 * another is expected.
 *
 * Auto-generated by `pnpm sync-tasks`. Do not edit by hand.
 */
/**
 * Reference to a "Azure" service connection by name.
 */

declare const __brand_AzureConnection: unique symbol;
export type AzureConnection = string & { readonly [__brand_AzureConnection]: void };

/**
 * Construct a typed reference to a "Azure" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function azureConnection(name: string): AzureConnection {
  return name as AzureConnection;
}

/**
 * Reference to a "AzureRM" service connection by name.
 */

declare const __brand_AzureRMConnection: unique symbol;
export type AzureRMConnection = string & { readonly [__brand_AzureRMConnection]: void };

/**
 * Construct a typed reference to a "AzureRM" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function azureRMConnection(name: string): AzureRMConnection {
  return name as AzureRMConnection;
}

/**
 * Reference to a "AzureServiceBus" service connection by name.
 */

declare const __brand_AzureServiceBusConnection: unique symbol;
export type AzureServiceBusConnection = string & { readonly [__brand_AzureServiceBusConnection]: void };

/**
 * Construct a typed reference to a "AzureServiceBus" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function azureServiceBusConnection(name: string): AzureServiceBusConnection {
  return name as AzureServiceBusConnection;
}

/**
 * Reference to a "ExternalNuGetFeed" service connection by name.
 */

declare const __brand_ExternalNuGetFeedConnection: unique symbol;
export type ExternalNuGetFeedConnection = string & { readonly [__brand_ExternalNuGetFeedConnection]: void };

/**
 * Construct a typed reference to a "ExternalNuGetFeed" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function externalNuGetFeedConnection(name: string): ExternalNuGetFeedConnection {
  return name as ExternalNuGetFeedConnection;
}

/**
 * Reference to a "ExternalTfs" service connection by name.
 */

declare const __brand_ExternalTfsConnection: unique symbol;
export type ExternalTfsConnection = string & { readonly [__brand_ExternalTfsConnection]: void };

/**
 * Construct a typed reference to a "ExternalTfs" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function externalTfsConnection(name: string): ExternalTfsConnection {
  return name as ExternalTfsConnection;
}

/**
 * Reference to a "Generic" service connection by name.
 */

declare const __brand_GenericConnection: unique symbol;
export type GenericConnection = string & { readonly [__brand_GenericConnection]: void };

/**
 * Construct a typed reference to a "Generic" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function genericConnection(name: string): GenericConnection {
  return name as GenericConnection;
}

/**
 * Reference to a "Jenkins" service connection by name.
 */

declare const __brand_JenkinsConnection: unique symbol;
export type JenkinsConnection = string & { readonly [__brand_JenkinsConnection]: void };

/**
 * Construct a typed reference to a "Jenkins" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function jenkinsConnection(name: string): JenkinsConnection {
  return name as JenkinsConnection;
}

/**
 * Reference to a "WorkloadIdentityUser" service connection by name.
 */

declare const __brand_WorkloadIdentityUserConnection: unique symbol;
export type WorkloadIdentityUserConnection = string & { readonly [__brand_WorkloadIdentityUserConnection]: void };

/**
 * Construct a typed reference to a "WorkloadIdentityUser" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function workloadIdentityUserConnection(name: string): WorkloadIdentityUserConnection {
  return name as WorkloadIdentityUserConnection;
}

/**
 * Reference to a "dockerhost" service connection by name.
 */

declare const __brand_dockerhostConnection: unique symbol;
export type dockerhostConnection = string & { readonly [__brand_dockerhostConnection]: void };

/**
 * Construct a typed reference to a "dockerhost" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function dockerhostConnection(name: string): dockerhostConnection {
  return name as dockerhostConnection;
}

/**
 * Reference to a "dockerregistry" service connection by name.
 */

declare const __brand_dockerregistryConnection: unique symbol;
export type dockerregistryConnection = string & { readonly [__brand_dockerregistryConnection]: void };

/**
 * Construct a typed reference to a "dockerregistry" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function dockerregistryConnection(name: string): dockerregistryConnection {
  return name as dockerregistryConnection;
}

/**
 * Reference to a "externalPythonDownloadFeed" service connection by name.
 */

declare const __brand_externalPythonDownloadFeedConnection: unique symbol;
export type externalPythonDownloadFeedConnection = string & { readonly [__brand_externalPythonDownloadFeedConnection]: void };

/**
 * Construct a typed reference to a "externalPythonDownloadFeed" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function externalPythonDownloadFeedConnection(name: string): externalPythonDownloadFeedConnection {
  return name as externalPythonDownloadFeedConnection;
}

/**
 * Reference to a "externalPythonUploadFeed" service connection by name.
 */

declare const __brand_externalPythonUploadFeedConnection: unique symbol;
export type externalPythonUploadFeedConnection = string & { readonly [__brand_externalPythonUploadFeedConnection]: void };

/**
 * Construct a typed reference to a "externalPythonUploadFeed" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function externalPythonUploadFeedConnection(name: string): externalPythonUploadFeedConnection {
  return name as externalPythonUploadFeedConnection;
}

/**
 * Reference to a "externalcargorepository" service connection by name.
 */

declare const __brand_externalcargorepositoryConnection: unique symbol;
export type externalcargorepositoryConnection = string & { readonly [__brand_externalcargorepositoryConnection]: void };

/**
 * Construct a typed reference to a "externalcargorepository" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function externalcargorepositoryConnection(name: string): externalcargorepositoryConnection {
  return name as externalcargorepositoryConnection;
}

/**
 * Reference to a "externalmavenrepository" service connection by name.
 */

declare const __brand_externalmavenrepositoryConnection: unique symbol;
export type externalmavenrepositoryConnection = string & { readonly [__brand_externalmavenrepositoryConnection]: void };

/**
 * Construct a typed reference to a "externalmavenrepository" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function externalmavenrepositoryConnection(name: string): externalmavenrepositoryConnection {
  return name as externalmavenrepositoryConnection;
}

/**
 * Reference to a "externalnpmregistry" service connection by name.
 */

declare const __brand_externalnpmregistryConnection: unique symbol;
export type externalnpmregistryConnection = string & { readonly [__brand_externalnpmregistryConnection]: void };

/**
 * Construct a typed reference to a "externalnpmregistry" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function externalnpmregistryConnection(name: string): externalnpmregistryConnection {
  return name as externalnpmregistryConnection;
}

/**
 * Reference to a "externaltfs" service connection by name.
 */

declare const __brand_externaltfsConnection: unique symbol;
export type externaltfsConnection = string & { readonly [__brand_externaltfsConnection]: void };

/**
 * Construct a typed reference to a "externaltfs" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function externaltfsConnection(name: string): externaltfsConnection {
  return name as externaltfsConnection;
}

/**
 * Reference to a "github" service connection by name.
 */

declare const __brand_githubConnection: unique symbol;
export type githubConnection = string & { readonly [__brand_githubConnection]: void };

/**
 * Construct a typed reference to a "github" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function githubConnection(name: string): githubConnection {
  return name as githubConnection;
}

/**
 * Reference to a "github:OAuth,OAuth2,PersonalAccessToken,InstallationToken,Token" service
 * connection by name.
 */

declare const __brand_github_OAuth_OAuth2_PersonalAccessToken_InstallationToken_TokenConnection: unique symbol;
export type github_OAuth_OAuth2_PersonalAccessToken_InstallationToken_TokenConnection = string & { readonly [__brand_github_OAuth_OAuth2_PersonalAccessToken_InstallationToken_TokenConnection]: void };

/**
 * Construct a typed reference to a
 * "github:OAuth,OAuth2,PersonalAccessToken,InstallationToken,Token" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function github_OAuth_OAuth2_PersonalAccessToken_InstallationToken_TokenConnection(name: string): github_OAuth_OAuth2_PersonalAccessToken_InstallationToken_TokenConnection {
  return name as github_OAuth_OAuth2_PersonalAccessToken_InstallationToken_TokenConnection;
}

/**
 * Reference to a "github:OAuth,PersonalAccessToken,InstallationToken,Token" service connection
 * by name.
 */

declare const __brand_github_OAuth_PersonalAccessToken_InstallationToken_TokenConnection: unique symbol;
export type github_OAuth_PersonalAccessToken_InstallationToken_TokenConnection = string & { readonly [__brand_github_OAuth_PersonalAccessToken_InstallationToken_TokenConnection]: void };

/**
 * Construct a typed reference to a "github:OAuth,PersonalAccessToken,InstallationToken,Token"
 * service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function github_OAuth_PersonalAccessToken_InstallationToken_TokenConnection(name: string): github_OAuth_PersonalAccessToken_InstallationToken_TokenConnection {
  return name as github_OAuth_PersonalAccessToken_InstallationToken_TokenConnection;
}

/**
 * Reference to a "kubernetes" service connection by name.
 */

declare const __brand_kubernetesConnection: unique symbol;
export type kubernetesConnection = string & { readonly [__brand_kubernetesConnection]: void };

/**
 * Construct a typed reference to a "kubernetes" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function kubernetesConnection(name: string): kubernetesConnection {
  return name as kubernetesConnection;
}

/**
 * Reference to a "servicefabric" service connection by name.
 */

declare const __brand_servicefabricConnection: unique symbol;
export type servicefabricConnection = string & { readonly [__brand_servicefabricConnection]: void };

/**
 * Construct a typed reference to a "servicefabric" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function servicefabricConnection(name: string): servicefabricConnection {
  return name as servicefabricConnection;
}

/**
 * Reference to a "ssh" service connection by name.
 */

declare const __brand_sshConnection: unique symbol;
export type sshConnection = string & { readonly [__brand_sshConnection]: void };

/**
 * Construct a typed reference to a "ssh" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function sshConnection(name: string): sshConnection {
  return name as sshConnection;
}

/**
 * Reference to a "vsmobilecenter" service connection by name.
 */

declare const __brand_vsmobilecenterConnection: unique symbol;
export type vsmobilecenterConnection = string & { readonly [__brand_vsmobilecenterConnection]: void };

/**
 * Construct a typed reference to a "vsmobilecenter" service connection.
 *
 * @param name The name (or id) of the service connection in the Azure DevOps project.
 */

export function vsmobilecenterConnection(name: string): vsmobilecenterConnection {
  return name as vsmobilecenterConnection;
}
